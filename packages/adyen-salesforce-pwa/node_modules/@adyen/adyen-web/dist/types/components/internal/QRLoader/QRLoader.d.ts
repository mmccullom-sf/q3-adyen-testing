import { Component, h } from 'preact';
import './QRLoader.scss';
import { QRLoaderProps, QRLoaderState } from './types';
declare class QRLoader extends Component<QRLoaderProps, QRLoaderState> {
    private timeoutId;
    constructor(props: any);
    static defaultProps: {
        delay: number;
        countdownTime: number;
        onError: () => void;
        onComplete: () => void;
        throttleTime: number;
        classNameModifiers: any[];
        throttledInterval: number;
        introduction: string;
        timeToPay: string;
        buttonLabel: string;
    };
    componentDidMount(): void;
    componentWillUnmount(): void;
    redirectToApp: (url: string | URL) => void;
    statusInterval: (responseTime?: number) => void;
    private pollStatus;
    private onTick;
    private onTimeUp;
    private onComplete;
    private onError;
    private checkStatus;
    render({ amount, url, brandLogo, brandName, countdownTime, type, onActionHandled }: QRLoaderProps, { expired, completed, loading }: {
        expired: any;
        completed: any;
        loading: any;
    }): h.JSX.Element;
}
export default QRLoader;
